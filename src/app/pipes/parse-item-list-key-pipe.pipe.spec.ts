import { ParseItemListKeyPipe } from './parse-item-list-key.pipe';

describe('ParseItemListKeyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ParseItemListKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
